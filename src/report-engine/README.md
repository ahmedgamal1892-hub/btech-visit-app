# Report Engine V2

Isolated report engine.

## Sprint 2

React report template (`VisitReportTemplate`) — HTML/CSS only.

## Sprint 3 — PDF proof of concept

Command:

```bash
npm run report-engine:sample-pdf
```

Output:

- `output/sample-report.pdf`
- `output/sample-report.html` (debug intermediate)

Uses Playwright + system Chrome to print the React template to A4 PDF.
